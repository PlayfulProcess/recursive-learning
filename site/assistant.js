/* assistant.js: a STUB, deliberately empty of behaviour. The tarot template mounts the shared
 * recursive.eco assistant here; v1 of this site leaves it out (no sign-in, no AI on the page).
 * The copied viewers load this file with `defer` and test for window.RecursiveAssistant before
 * using it, so nothing else is needed. To bring the assistant back, copy
 * recursive-tarot/assistant.js over this file. */
