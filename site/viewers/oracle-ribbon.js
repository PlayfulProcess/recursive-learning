/* oracle-ribbon.js: a STUB. The tarot template shows a rolling "oracle" line under casts and in
 * the footer; v1 of this site leaves it out. The footer and the caster call
 * window.OracleRibbon.show(anchor, opts) when it exists, so the stub defines that call and does
 * nothing. To bring the ribbon back, copy recursive-tarot/viewers/oracle-ribbon.js over this file. */
(function () {
  if (window.OracleRibbon) return;
  window.OracleRibbon = { show: function () {} };
})();
